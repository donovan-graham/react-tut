const initialState = {
  currentStep: 1,
  isLoading: false,
};

const additionalContribution = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default additionalContribution;
